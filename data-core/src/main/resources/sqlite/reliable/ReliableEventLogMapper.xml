<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.juphoon.rtc.datacenter.binlog.mapper.reliable.ReliableEventLogMapper">

    <resultMap id="eventPoResultMap" type="com.juphoon.rtc.datacenter.binlog.entity.EventBinLogPO">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="requestId" property="requestId" jdbcType="VARCHAR"/>
        <result column="from" property="from" jdbcType="VARCHAR"/>
        <result column="processorId" property="processorId" jdbcType="VARCHAR"/>
        <result column="receivedTimestamp" property="receivedTimestamp" jdbcType="INTEGER"/>
        <result column="redoHandler" property="redoHandler" jdbcType="VARCHAR"/>
        <result column="retryCount" property="retryCount" jdbcType="INTEGER"/>

        <result column="uuid" property="uuid" jdbcType="VARCHAR"/>
        <result column="domainId" property="domainId" jdbcType="INTEGER"/>
        <result column="appId" property="appId" jdbcType="INTEGER"/>
        <result column="type" property="type" jdbcType="INTEGER"/>
        <result column="number" property="number" jdbcType="INTEGER"/>
        <result column="timestamp" property="timestamp" jdbcType="INTEGER"/>
        <result column="params" property="params" jdbcType="LONGVARCHAR"/>

    </resultMap>

    <update id="dropTable">
        DROP TABLE IF EXISTS event_binlog;
    </update>

    <update id="createTable">
        create table IF NOT EXISTS `event_binlog`
        (
            `id`          integer not null constraint event_log_pk primary key,
            `requestId`   varchar(32) not null,
            `from`        varchar(64) not null,
            `processorId` varchar(32) not null,
            `receivedTimestamp`   integer not null,
            `redoHandler` varchar(32),
            `retryCount`  int not null,

            `uuid`      varchar(32) not null,
            `domainId`  int         not null,
            `appId`     int         not null,
            `type`      int         not null,
            `number`    int         not null,
            `timestamp` integer     not null,
            `params`    text
        );
    </update>

    <insert id="save"
            parameterType="com.juphoon.rtc.datacenter.binlog.entity.EventBinLogPO"
            statementType="PREPARED">
        INSERT INTO `event_binlog`(`id`, `requestId`, `from`, `processorId`, `receivedTimestamp`, `redoHandler`,
        `retryCount`, `domainId`, `appId`, `uuid`, `type`, `number`, `timestamp`, `params`)
        VALUES (#{id}, #{requestId}, #{from}, #{processorId}, #{receivedTimestamp}, #{redoHandler},
        #{retryCount}, #{domainId}, #{appId}, #{uuid}, #{type}, #{number}, #{timestamp}, #{params});
    </insert>

    <insert id="saveList">
        INSERT INTO `event_binlog`(`id`, `requestId`, `from`, `processorId`, `receivedTimestamp`, `redoHandler`,
        `retryCount`, `domainId`, `appId`, `uuid`, `type`, `number`, `timestamp`, `params`) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.requestId}, #{item.from}, #{item.processorId}, #{item.receivedTimestamp},
            #{item.redoHandler},
            #{item.retryCount}, #{item.domainId}, #{item.appId}, #{item.uuid}, #{item.type}, #{item.number},
            #{item.timestamp}, #{item.params})
        </foreach>
    </insert>

    <delete id="remove">
        DELETE FROM `event_binlog` WHERE `id`=#{id};
    </delete>

    <select id="findById" resultMap="eventPoResultMap">
        SELECT `id`, `requestId`, `from`, `processorId`, `receivedTimestamp`, `redoHandler`, `retryCount`, `domainId`, `appId`, `uuid`, `type`, `number`, `timestamp`, `params`
        FROM `event_binlog`
        WHERE `id`=#{id};
    </select>

    <select id="find" resultMap="eventPoResultMap">
        SELECT `id`, `requestId`, `from`, `processorId`, `receivedTimestamp`, `redoHandler`, `retryCount`, `domainId`,
        `appId`, `uuid`, `type`, `number`, `timestamp`, `params`
        FROM `event_binlog`
        ORDER BY `timestamp`
        LIMIT #{size};
    </select>
</mapper>